# Python dependencies are managed with uv (see services/embedder/pyproject.toml).
# Generate a requirements export if needed via:
#   cd services/embedder && uv export --format requirements --locked > ../requirements-export.txt
